<?xml version="1.0" encoding="UTF-8"?>
<!--
  Kaynos Docs is a single-page application (SPA) — all content is served from
  index.html and accessed via hash fragments (e.g. /#setup). Crawlers strip the
  fragment, so listing fragment URLs only dilutes crawl budget. Until KAY-182
  introduces path-based routing with server-side rendering, this sitemap lists
  only the canonical root URL.
-->
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url><loc>https://docs.kaynos.net/</loc><lastmod>2026-05-12</lastmod><priority>1.0</priority></url>
</urlset>
